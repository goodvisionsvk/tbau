<!DOCTYPE html>
<html lang="sk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> · <%= company.shortName %> portál</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/static/css/style.css">
  <link rel="icon" type="image/png" href="/static/img/logo.png">
</head>
<body class="app">
  <header class="topbar">
    <div class="topbar-left">
      <% if (currentUser) { %>
        <button class="nav-toggle" type="button" aria-label="Otvoriť menu" aria-expanded="false">☰</button>
      <% } %>
      <a class="brand" href="<%= currentUser ? '/dashboard' : '/' %>"><img class="brand-logo" src="/static/img/logo.png" alt="TBAU"></a>
    </div>
    <div class="topbar-right">
      <% if (currentUser) { %>
        <span class="user-chip">
          <%= currentUser.full_name || currentUser.email %>
          <% if (currentUser.role === 'admin') { %><span class="badge">admin</span><% } %>
        </span>
        <a class="btn-link hide-mobile" href="/account/password">Zmena hesla</a>
        <form class="inline" method="post" action="/logout">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <button class="btn btn-ghost btn-sm">Odhlásiť</button>
        </form>
      <% } else { %>
        <a class="btn btn-primary btn-sm" href="/login">Prihlásiť sa</a>
      <% } %>
    </div>
  </header>
  <div class="shell">
    <% if (currentUser) { %>
    <aside class="sidebar" id="sidebar">
      <nav>
        <% nav.forEach(function(item){
             if (item.adminOnly && currentUser.role !== 'admin') return; %>
          <a class="nav-item <%= active === item.key ? 'active' : '' %>" href="<%= item.href %>">
            <span class="nav-ico"><%= item.icon %></span><%= item.label %>
          </a>
        <% }) %>
      </nav>
      <div class="sidebar-account">
        <a class="nav-item" href="/account/password"><span class="nav-ico">🔑</span>Zmena hesla</a>
      </div>
      <div class="sidebar-foot"><%= company.name %><br>IČO <%= company.ico %></div>
    </aside>
    <div class="nav-backdrop" hidden></div>
    <% } %>
    <main class="content">
      <div class="page-head"><h1><%= title %></h1></div>
      <%- body %>
    </main>
  </div>
  <% if (currentUser) { %><script src="/static/js/app.js" defer></script><% } %>
</body>
</html>
